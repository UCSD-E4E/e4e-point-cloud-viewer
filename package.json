{
  "name": "e4e-point-cloud-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "fixture": "cargo run -q -p e4epc-baker --example bake -- crates/e4epc-baker/tests/fixtures/autzen_trim.laz public/fixtures/autzen.e4epc",
    "test": "vitest",
    "test:run": "vitest run --passWithNoTests",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/three": "^0.170.0",
    "@vitest/ui": "^4",
    "happy-dom": "^20",
    "typescript": "^5.6.0",
    "vite": "^6",
    "vitest": "^4"
  }
}
